let selectedInterval='month';document.querySelectorAll('[data-interval]').forEach(button=>button.addEventListener('click',()=>{selectedInterval=button.dataset.interval;document.querySelectorAll('[data-interval]').forEach(x=>x.classList.toggle('active',x===button));document.querySelectorAll('.price').forEach(price=>{price.innerHTML=`${price.dataset[selectedInterval]} <small>/month</small>`});document.querySelectorAll('.price-sub').forEach(copy=>copy.textContent=copy.dataset[`${selectedInterval}Copy`])}));document.querySelectorAll('[data-plan]').forEach(button=>button.addEventListener('click',()=>checkout(button.dataset.plan,selectedInterval)));if(window.lucide)lucide.createIcons();
