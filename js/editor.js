let currentId=null,user=null,saveTimer=null,saveInFlight=false,profileUrl='',subscription=null,currentPlan='starter',templates=[],socialLinks=[];
const fieldNames=['full_name','job_title','company_name','biography','phone','email','website','business_address','headline','primary_color','secondary_color','background_color','text_color','font_family','button_style','profile_image_shape','border_radius','slug','template_id','profile_image_url'];
const socialPlatforms=['Instagram','Facebook','LinkedIn','TikTok','YouTube','X','WhatsApp','Pinterest','Website'];
(async()=>{user=await requireUser();if(!user)return;const [{data:sub},{data:templateData}]=await Promise.all([supabaseClient.from('subscriptions').select('plan_key,status').eq('user_id',user.id).maybeSingle(),supabaseClient.from('templates').select('*').eq('is_active',true).order('is_premium').order('name')]);subscription=sub;currentPlan=sub?.plan_key||'starter';const canRemoveBranding=['pro','agency'].includes(currentPlan)&&['active','trialing','past_due'].includes(subscription?.status);if(!canRemoveBranding){field('show_branding').checked=true;field('show_branding').disabled=true}templates=templateData||[];renderTemplates();const params=new URLSearchParams(location.search);currentId=params.get('id');if(currentId)await loadCard();else initializeNewCard();wireEvents();render();updateCompletion();if(window.lucide)lucide.createIcons()})();
function field(name){return document.querySelector(`[name="${name}"]`)}function value(name){const el=field(name);if(!el)return'';return el.type==='checkbox'?el.checked:el.value||''}
function setSaveState(state,text){const el=document.getElementById('save-state');el.className=`save-state ${state}`;el.innerHTML=`<i data-lucide="${state==='saving'?'loader-circle':'check-circle-2'}" size="16"></i> ${text}`;if(window.lucide)lucide.createIcons()}
function initializeNewCard(){field('primary_color').value='#5b5cf0';field('secondary_color').value='#9b5de5';field('background_color').value='#ffffff';field('text_color').value='#111827';field('status').value='draft';addSocialRow()}
async function loadCard(){const {data,error}=await supabaseClient.from('digital_cards').select('*').eq('id',currentId).single();if(error){toast(error.message);return}fieldNames.forEach(name=>{const el=field(name);if(!el||data[name]===null||data[name]===undefined)return;if(el.type==='checkbox')el.checked=Boolean(data[name]);else el.value=data[name]});field('show_branding').checked=field('show_branding').disabled?true:data.show_branding!==false;field('status').value=data.status;document.querySelectorAll('.template-card').forEach(x=>x.classList.toggle('active',x.dataset.template===String(data.template_id||'')));profileUrl=data.profile_image_url||'';updatePhoto();const {data:links}=await supabaseClient.from('social_links').select('*').eq('card_id',currentId).order('sort_order');socialLinks=links||[];renderSocialRows();updatePublicControls()}
function wireEvents(){document.querySelectorAll('.editor-tab').forEach(tab=>tab.addEventListener('click',()=>openTab(tab.dataset.tab)));document.querySelectorAll('input,textarea,select').forEach(el=>{if(['profile-file'].includes(el.id))return;el.addEventListener('input',()=>{render();updateCompletion();scheduleSave()});el.addEventListener('change',()=>{render();scheduleSave()})});document.getElementById('bio').addEventListener('input',()=>document.getElementById('bio-count').textContent=value('biography').length);document.getElementById('profile-file').addEventListener('change',uploadPhoto);document.getElementById('remove-photo').addEventListener('click',()=>{profileUrl='';field('profile_image_url').value='';updatePhoto();render();scheduleSave()});document.getElementById('add-social').addEventListener('click',()=>addSocialRow());document.getElementById('publish-button').addEventListener('click',togglePublish);document.getElementById('panel-publish-button').addEventListener('click',togglePublish);document.getElementById('copy-card-link').addEventListener('click',copyCardLink);document.getElementById('download-qr').addEventListener('click',()=>window.open(document.getElementById('editor-qr').src,'_blank'));document.getElementById('mobile-preview-button').addEventListener('click',async()=>{await waitForSave();if(!currentId)await save({silent:true});if(currentId)window.open(cardUrl(),'_blank','noopener')});document.getElementById('slug').addEventListener('input',()=>{field('slug').value=slugify(field('slug').value);render()});document.querySelectorAll('.color-preset').forEach(button=>button.addEventListener('click',()=>{const [primary,secondary,bg,text]=button.dataset.colors.split(',');field('primary_color').value=primary;field('secondary_color').value=secondary;field('background_color').value=bg;field('text_color').value=text;document.querySelectorAll('.color-preset').forEach(x=>x.classList.remove('active'));button.classList.add('active');render();scheduleSave()}));document.querySelector('[data-close-dialog]').addEventListener('click',()=>document.getElementById('upgrade-dialog').close())}
function openTab(name){document.querySelectorAll('.editor-tab').forEach(x=>x.classList.toggle('active',x.dataset.tab===name));document.querySelectorAll('.editor-panel').forEach(x=>x.classList.toggle('active',x.dataset.panel===name))}
function render(){const full=value('full_name')||'Your Name';const initials=full.split(/\s+/).map(x=>x[0]).slice(0,2).join('').toUpperCase()||'YN';document.getElementById('p-name').textContent=full;document.getElementById('p-title').textContent=value('job_title')||'Your position';document.getElementById('p-company').textContent=value('company_name')||'Your company';document.getElementById('p-headline').textContent=value('headline');document.getElementById('p-bio').textContent=value('biography')||'A short introduction about your business will appear here.';document.getElementById('preview-initials').textContent=initials;const primary=value('primary_color')||'#5b5cf0',secondary=value('secondary_color')||'#9b5de5',bg=value('background_color')||'#ffffff',text=value('text_color')||'#111827';document.getElementById('preview-cover').style.background=`linear-gradient(135deg,${primary},${secondary})`;const phone=document.getElementById('phone-preview');phone.style.background=bg;phone.style.color=text;document.querySelectorAll('.preview-action').forEach(button=>{button.style.borderRadius=`${value('border_radius')||16}px`;if(value('button_style')==='outline'){button.style.background='transparent';button.style.border=`1px solid ${primary}`;button.style.color=primary}else if(value('button_style')==='soft'){button.style.background=`${primary}20`;button.style.border='0';button.style.color=primary}else{button.style.background=primary;button.style.border='0';button.style.color='#fff'}});const avatar=document.getElementById('preview-avatar');avatar.style.borderRadius=value('profile_image_shape')==='square'?'8px':value('profile_image_shape')==='rounded'?'25px':'50%';phone.style.fontFamily=value('font_family')||'DM Sans';document.getElementById('preview-branding').hidden=!field('show_branding').checked;const slug=value('slug')||slugify(full)||'your-name';document.getElementById('slug-value').textContent=slug;document.getElementById('preview-status').textContent=value('status')==='published'?'Published':'Draft';renderPreviewSocials();updatePublicControls()}
function updatePhoto(){field('profile_image_url').value=profileUrl;const containers=[document.getElementById('photo-preview'),document.getElementById('preview-avatar')];containers.forEach((el,index)=>{const initials=(value('full_name')||'YN').split(/\s+/).map(x=>x[0]).slice(0,2).join('').toUpperCase();el.innerHTML=profileUrl?`<img src="${escapeHtml(profileUrl)}" alt="Profile preview">`:(index?`<span id="preview-initials">${initials}</span>`:'<i data-lucide="user-round" size="28"></i>')});if(window.lucide)lucide.createIcons()}
async function uploadPhoto(event){const file=event.target.files?.[0];if(!file)return;if(file.size>5*1024*1024)return toast('Photo must be smaller than 5 MB');const safe=file.name.toLowerCase().replace(/[^a-z0-9.]+/g,'-');const path=`${user.id}/${Date.now()}-${safe}`;setSaveState('saving','Uploading photo…');const {error}=await supabaseClient.storage.from('profile-images').upload(path,file,{cacheControl:'3600',upsert:false});if(error){setSaveState('saved','Saved');return toast(error.message)}const {data}=supabaseClient.storage.from('profile-images').getPublicUrl(path);profileUrl=data.publicUrl;field('profile_image_url').value=profileUrl;updatePhoto();render();await save({silent:true});toast('Photo uploaded')}
function renderTemplates(){const allowed=['pro','agency'].includes(currentPlan)&&['active','trialing','past_due'].includes(subscription?.status);const grid=document.getElementById('template-grid');grid.innerHTML=templates.map(t=>`<button type="button" class="template-card ${t.is_premium&&!allowed?'locked':''}" data-template="${t.id}" data-premium="${t.is_premium}"><div class="template-preview" style="background:${templateGradient(t.template_key)}"></div><strong>${escapeHtml(t.name)}</strong>${t.is_premium?'<span class="template-lock"><i data-lucide="lock" size="14"></i></span>':''}</button>`).join('');grid.querySelectorAll('.template-card').forEach(button=>button.addEventListener('click',()=>{if(button.dataset.premium==='true'&&!allowed){toast('Premium templates are included with Pro and Agency');return}field('template_id').value=button.dataset.template;grid.querySelectorAll('.template-card').forEach(x=>x.classList.remove('active'));button.classList.add('active');scheduleSave()}));if(window.lucide)lucide.createIcons()}
function templateGradient(key){const map={corporate:'linear-gradient(135deg,#172554,#3b82f6)',minimal:'linear-gradient(135deg,#f8fafc,#dbeafe)',creative:'linear-gradient(135deg,#7c3aed,#ec4899)',luxury:'linear-gradient(135deg,#111827,#b7791f)','real-estate':'linear-gradient(135deg,#0f766e,#2dd4bf)',beauty:'linear-gradient(135deg,#be185d,#f9a8d4)',healthcare:'linear-gradient(135deg,#0369a1,#38bdf8)',restaurant:'linear-gradient(135deg,#7f1d1d,#f59e0b)',contractor:'linear-gradient(135deg,#292524,#f97316)',automotive:'linear-gradient(135deg,#111827,#ef4444)',daycare:'linear-gradient(135deg,#0ea5e9,#f9a8d4)','credit-repair':'linear-gradient(135deg,#166534,#22c55e)'};return map[key]||'linear-gradient(135deg,#5b5cf0,#9b5de5)'}
function addSocialRow(link={platform:'Instagram',url:''}){socialLinks.push(link);renderSocialRows()}
function renderSocialRows(){const list=document.getElementById('social-list');if(!socialLinks.length)socialLinks=[{platform:'Instagram',url:''}];list.innerHTML=socialLinks.map((link,index)=>`<div class="social-row" data-social-index="${index}"><select class="input" aria-label="Social platform">${socialPlatforms.map(p=>`<option ${p.toLowerCase()===String(link.platform).toLowerCase()?'selected':''}>${p}</option>`).join('')}</select><input class="input" type="url" placeholder="https://..." value="${escapeHtml(link.url||'')}"><button type="button" class="icon-btn" aria-label="Remove link"><i data-lucide="trash-2" size="17"></i></button></div>`).join('');list.querySelectorAll('.social-row').forEach(row=>{const index=Number(row.dataset.socialIndex);const [select,input,button]=row.children;select.addEventListener('change',()=>{socialLinks[index].platform=select.value;render();scheduleSave()});input.addEventListener('input',()=>{socialLinks[index].url=input.value;render();scheduleSave()});button.addEventListener('click',()=>{socialLinks.splice(index,1);renderSocialRows();render();scheduleSave()})});if(window.lucide)lucide.createIcons()}
function renderPreviewSocials(){const area=document.getElementById('preview-socials');const visible=socialLinks.filter(x=>x.url).slice(0,5);area.innerHTML=visible.map(x=>`<span class="preview-social" title="${escapeHtml(x.platform)}"><i data-lucide="${platformIcon(x.platform)}" size="16"></i></span>`).join('');if(window.lucide)lucide.createIcons()}
function platformIcon(platform){const key=String(platform).toLowerCase();return({instagram:'instagram',facebook:'facebook',linkedin:'linkedin',youtube:'youtube',whatsapp:'message-circle',website:'globe',tiktok:'music-2',x:'at-sign',pinterest:'pin'})[key]||'link'}
function scheduleSave(){clearTimeout(saveTimer);setSaveState('saving','Unsaved changes');saveTimer=setTimeout(()=>save({silent:true}),750)}
async function save({silent=false}={}){if(saveInFlight)return;saveInFlight=true;setSaveState('saving','Saving…');try{const payload={user_id:user.id,show_branding:field('show_branding').checked,status:value('status')||'draft'};fieldNames.forEach(name=>{const val=value(name);payload[name]=val===''?null:val});payload.profile_image_url=profileUrl||null;if(!payload.slug)payload.slug=`${slugify(payload.full_name||'card')}-${Math.random().toString(36).slice(2,6)}`;let result=currentId?await supabaseClient.from('digital_cards').update(payload).eq('id',currentId).select().single():await supabaseClient.from('digital_cards').insert(payload).select().single();if(result.error)throw result.error;currentId=result.data.id;history.replaceState({},'',`editor.html?id=${currentId}`);field('slug').value=result.data.slug;field('status').value=result.data.status;await saveSocialLinks();setSaveState('saved','Saved');updatePublicControls();if(!silent)toast('Card saved')}catch(error){setSaveState('saved','Could not save');toast(error.message)}finally{saveInFlight=false}}
async function saveSocialLinks(){if(!currentId)return;const clean=socialLinks.filter(x=>x.url?.trim()).map((x,index)=>({card_id:currentId,platform:x.platform.toLowerCase(),label:x.platform,url:x.url.trim(),is_enabled:true,sort_order:index}));const {error:deleteError}=await supabaseClient.from('social_links').delete().eq('card_id',currentId);if(deleteError)throw deleteError;if(clean.length){const {error}=await supabaseClient.from('social_links').insert(clean);if(error)throw error}}
async function togglePublish(){await waitForSave();if(!currentId)await save({silent:true});if(!currentId)return;const active=['active','trialing','past_due'].includes(subscription?.status);if(!active){document.getElementById('upgrade-dialog').showModal();return}const next=value('status')==='published'?'draft':'published';field('status').value=next;await save({silent:true});render();toast(next==='published'?'Your card is live':'Card returned to draft')}
function updatePublicControls(){const published=value('status')==='published',url=cardUrl();document.getElementById('publish-button').innerHTML=published?'<i data-lucide="pause-circle" size="17"></i> Unpublish':'<i data-lucide="rocket" size="17"></i> Publish';document.getElementById('panel-publish-button').textContent=published?'Unpublish card':'Publish card';document.getElementById('publish-title').textContent=published?'Your card is live':'Your card is a draft';document.getElementById('publish-copy').textContent=published?'Customers with your link can view it now.':'Only you can preview it until it is published.';document.getElementById('share-tools').hidden=!published;const preview=document.getElementById('preview-link');preview.hidden=!currentId;preview.href=url;if(published){const qr=`https://api.qrserver.com/v1/create-qr-code/?size=420x420&data=${encodeURIComponent(url)}`;document.getElementById('editor-qr').src=qr}if(window.lucide)lucide.createIcons()}
function waitForSave(){return new Promise(resolve=>{if(!saveInFlight)return resolve();const timer=setInterval(()=>{if(!saveInFlight){clearInterval(timer);resolve()}},50)})}function cardUrl(){return dotcoUrl(`card.html?slug=${encodeURIComponent(value('slug')||'')}`)}async function copyCardLink(){await waitForSave();if(!currentId)await save({silent:true});if(!currentId)return;await navigator.clipboard.writeText(cardUrl());toast('Card link copied')}
function updateCompletion(){const required=['full_name','job_title','company_name','phone','email','biography'];const filled=required.filter(name=>value(name)).length;const percent=Math.round(filled/required.length*100);document.getElementById('completion-label').textContent=`${percent}% complete`;document.getElementById('bio-count').textContent=value('biography').length}
function slugify(text){return String(text||'').toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'').slice(0,60)}
