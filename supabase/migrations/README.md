# Applied migrations

The live DotCo Cards project already contains the core schema, RLS policies, storage buckets, plan limits, templates, analytics tables, billing tables, and Stripe price links.

Project ref: `bmgmdgkviqqtaluvdprh`

Use `supabase db pull initial_dotco_schema --linked` to pull the exact live schema into this folder before future local development.
